generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("ALCOVES_API_DATABASE_URL")
}

model User {
  id    String @id @default(uuid())
  email String @unique
}
